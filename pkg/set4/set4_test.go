package set4

import (
	"testing"
)

func TestRandomAccessReadWriteAESCTR_Skip(t *testing.T) {
	// filename := "../../challenges/challenge25.txt"
	// decoded, err := utils.ReadBase64File(filename)
	// key := utils.GenerateKey()
	// e := pals.CTR{Plain: pals.Plain{
	// 	Plaintext:      decoded,
	// 	Key: Key,
	// }, Nonce: nonce}

	// if got != want {
	// 	t.Errorf("ToBase64(%q) == %q, want %q", in, got, want)
	// }
}
